# Інструкція: Завантаження ENGYR на GitHub

## Спосіб 1: Через GitHub Desktop (найпростіший)

1. **Завантажте GitHub Desktop** з https://desktop.github.com
2. Відкрийте GitHub Desktop
3. File → Add Local Repository → виберіть папку `engyr-project`
4. Натисніть "Publish repository"
5. Введіть назву: `engyr`
6. Зніміть галочку "Keep this code private" (якщо хочете публічний)
7. Натисніть "Publish repository"

✅ Готово! Ваш код на GitHub.

---

## Спосіб 2: Через командний рядок

### Крок 1: Відкрийте термінал в папці проекту

```bash
cd шлях/до/engyr-project
```

### Крок 2: Запустіть скрипт

```bash
./deploy-to-github.sh
```

Або виконайте команди вручну:

```bash
# Ініціалізація
git init

# Налаштування (якщо потрібно)
git config user.name "Ваше Ім'я"
git config user.email "ваш@email.com"

# Додавання файлів
git add .

# Коміт
git commit -m "Initial commit: ENGYR store"

# Перейменування гілки
git branch -M main

# Додавання remote (замініть USERNAME на ваш GitHub username)
git remote add origin https://github.com/USERNAME/engyr.git

# Завантаження
git push -u origin main
```

---

## Спосіб 3: Через веб-інтерфейс GitHub

1. Перейдіть на https://github.com
2. Натисніть "+" → "New repository"
3. Назвіть: `engyr`
4. Натисніть "Create repository"
5. На сторінці репозиторію натисніть "uploading an existing file"
6. Перетягніть всі файли з папки `engyr-project`
7. Напишіть коміт: "Initial commit"
8. Натисніть "Commit changes"

---

## Підключення до Netlify

Після завантаження на GitHub:

1. Перейдіть на https://netlify.com
2. Натисніть "Add new site" → "Import an existing project"
3. Виберіть "Deploy with GitHub"
4. Знайдіть репозиторій `engyr`
5. Налаштування залиште за замовчуванням
6. Натисніть "Deploy site"

### Налаштування Netlify Forms

1. Після розгортання перейдіть у Site settings
2. Forms → Form notifications
3. Додайте email: dm.21@outlook.com
4. Збережіть

✅ Готово! Ваш магазин онлайн на власному домені Netlify!

---

## Оновлення сайту в майбутньому

Коли ви змінюєте файли:

```bash
git add .
git commit -m "Опис змін"
git push
```

Netlify автоматично оновить сайт!

---

Якщо виникнуть питання - пишіть! 🚀
